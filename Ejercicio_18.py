def calcularMaxMin(numeros):
    lista = []
    for i in range(numeros):
        numeros =+0
        nums = int(input("Ingresa Numeros a la lista: "))
        lista.append(nums)
        print(lista)
    print(max(lista))
    print(min(lista))
    return

calcularMaxMin(6)